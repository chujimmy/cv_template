version: '3.8'

services:
  cv:
    image: "chujimmy/moderncv:latest"
    container_name: cv
    volumes:
      - ./src/:/data/cv/src/
      - ./entrypoint.sh/:/data/entrypoint.sh
    tty: true
    entrypoint: /bin/sh -c
    command: "/data/entrypoint.sh"
